This was a group task, so the code might look a bit messy in some (or many) places. Can't remember if I cleaned it up

The task was to implement a quiz with a set pool of questions. The outcome is more or less acceptable, although not quite what was expected